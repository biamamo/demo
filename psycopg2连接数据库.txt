import psycopg2

conn = psycopg2.connect(
    host="192.168.2.100",    # NAS IP
    port=5432,               # PostgreSQL 端口
    user="postgres",         # 你设置的用户名
    password="yourpassword", # 你设置的密码
    database="ais_project"   # 你创建的业务数据库名
)

cur = conn.cursor()
cur.execute("SELECT * FROM ais_data LIMIT 10;")
rows = cur.fetchall()
for row in rows:
    print(row)


# yaml

# host: 192.168.2.100
# port: 5432
# user: gitea
# password: 123456
# database: gitea
